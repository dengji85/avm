# 片匣 · AVM v1.9.0

发布日期：2026-08-09

## ✨ 新功能

- **AV-Wiki 源后台自动抓取**：程序自管理常驻 headless Chrome，**完全后台运行，无需手动开浏览器**，首次运行即可绕过反爬验证页，可纳入常规后台采集任务。
- **素人片封面补全**：JavBus 源详情页缺封面时自动回退取图，实测为库存补回大量此前缺失的素人片封面。

## 🔧 改进

- **筛选侧栏体验升级**：类型 / 标记 / 评分 / 年份等常用维度置顶常驻；女优 / 厂商 / 系列等长尾维度默认折叠（展示前 12 热门 + 总数角标）；组内条目过多时显示组内搜索框；已选条件自动置顶。

## 📦 下载与安装

1. 下载 `AVM-win64-v1.9.0.zip` 并解压到任意目录（建议非系统盘，如 `D:\AVM\`）。
2. 双击 `AVM.exe` 启动，程序会自动打开浏览器进入 Web UI。
3. 数据（库文件 / 封面 / 配置）保存在 `AVM.exe` 同级的 `data/` 目录，整体拷贝即可备份迁移。

> 发布包已包含前端静态资源与全部后端模块，无需安装 Python 或 Node。
> AV-Wiki 后台抓取功能需在设置中配置本机 Chrome 调试端口（需本机安装 Chrome）。
